import 'package:flutter/material.dart';
import 'aplikasi.dart';

void main() => runApp(
    MaterialApp(
      theme: ThemeData(
        primarySwatch: Colors.cyan,
      ),
      home:  InputAPK(),
    )
);
